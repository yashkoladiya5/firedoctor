name: firedoctor
version: 0.1.0
description: >
  Firebase diagnostics tool for Flutter projects.
  Automatically detects misconfigurations in Firebase setup across Android, iOS,
  FCM, Crashlytics, and more. CI/CD ready with deterministic exit codes.
repository: https://github.com/firedoctor-cli/firedoctor
homepage: https://github.com/firedoctor-cli/firedoctor
issue_tracker: https://github.com/firedoctor-cli/firedoctor/issues
documentation: https://github.com/firedoctor-cli/firedoctor/tree/main/docs
environment:
  sdk: '>=3.10.0 <4.0.0'
dependencies:
  args: ^2.4.0
  meta: ^1.11.0
  path: ^1.9.0
  yaml: ^3.1.0
dev_dependencies:
  test: ^1.25.0
  mocktail: ^1.0.0
  lints: ^3.0.0
