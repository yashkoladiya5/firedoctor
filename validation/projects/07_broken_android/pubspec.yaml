name: broken_android
version: 1.0.0
description: Project with intentionally broken Android Firebase configuration for validation.
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'
dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.20.0
